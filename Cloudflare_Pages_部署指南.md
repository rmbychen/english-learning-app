# Cloudflare Pages 部署指南（国内访问推荐）

## 🎯 为什么选择 Cloudflare Pages？

- ✅ **完全免费**，无需信用卡
- ✅ **全球 CDN**，在中国访问速度较好
- ✅ **自动部署**，连接 GitHub 后自动更新
- ✅ **支持自定义域名**
- ✅ **构建速度快**

---

## 📋 部署步骤

### 步骤 1：注册 Cloudflare 账号

1. 访问：https://dash.cloudflare.com/sign-up
2. 输入邮箱和密码注册
3. 验证邮箱（如果需要）

---

### 步骤 2：连接 GitHub 仓库

1. **登录 Cloudflare Dashboard**
   - 访问：https://dash.cloudflare.com

2. **进入 Pages**
   - 在左侧菜单找到 **Workers & Pages**
   - 点击 **Pages**
   - 点击 **Create application**

3. **连接 Git**
   - 选择 **Connect to Git**
   - 选择 **GitHub**
   - 授权 Cloudflare 访问你的 GitHub 账号
   - 选择仓库：`rmbychen/english-learning-app`
   - 点击 **Begin setup**

---

### 步骤 3：配置构建设置

在配置页面填写：

**Project name:**
```
english-learning-app
```

**Production branch:**
```
main
```

**Framework preset:**
```
None (或选择 Vite，但需要手动配置)
```

**Build command:**
```
cd english-learning-app && npm install && npm run build
```

**Build output directory:**
```
english-learning-app/dist
```

**Root directory:**
```
留空（或填写 `/`）
```

**Environment variables（如果需要）:**
- 如果你的 Supabase 使用环境变量，在这里添加：
  - `VITE_SUPABASE_URL`
  - `VITE_SUPABASE_ANON_KEY`

---

### 步骤 4：部署

1. 点击 **Save and Deploy**
2. 等待构建完成（通常 2-3 分钟）
3. 构建完成后会显示部署成功

---

### 步骤 5：获取访问地址

部署完成后，你会得到一个 URL，例如：
```
https://english-learning-app.pages.dev
```

**这个地址在中国访问速度会好很多！**

---

## 🔄 自动部署

配置完成后，每次你推送代码到 GitHub：
```bash
git push
```

Cloudflare Pages 会自动：
1. 检测到新的推送
2. 自动构建项目
3. 自动部署新版本

---

## 🌐 自定义域名（可选）

如果你想使用自己的域名：

1. 在 Cloudflare Pages 项目设置中
2. 进入 **Custom domains**
3. 添加你的域名
4. 按照提示配置 DNS 记录

---

## ⚙️ 配置说明

### 为什么需要这些配置？

**Build command:**
```
cd english-learning-app && npm install && npm run build
```
- 因为项目在 `english-learning-app` 子目录中
- 需要先进入目录，安装依赖，然后构建

**Build output directory:**
```
english-learning-app/dist
```
- Vite 构建后的文件在 `english-learning-app/dist` 目录

**Root directory:**
```
留空
```
- 项目根目录就是仓库根目录

---

## 🐛 常见问题

### 1. 构建失败

**检查：**
- Build command 是否正确
- Build output directory 是否正确
- 查看构建日志中的错误信息

### 2. 404 错误

**检查：**
- Build output directory 是否指向 `dist` 目录
- 确保 `dist/index.html` 存在

### 3. 访问速度仍然慢

**尝试：**
- 清除浏览器缓存
- 使用不同的网络环境测试
- 考虑使用自定义域名 + Cloudflare DNS

---

## 📊 与 Vercel 对比

| 特性 | Vercel | Cloudflare Pages |
|------|--------|------------------|
| 免费额度 | ✅ 充足 | ✅ 充足 |
| 国内访问 | ⚠️ 较慢 | ✅ 较好 |
| 构建速度 | ⚡ 快 | ⚡ 快 |
| 自动部署 | ✅ | ✅ |
| 自定义域名 | ✅ | ✅ |

---

## ✅ 完成后的检查清单

- [ ] Cloudflare 账号已注册
- [ ] GitHub 仓库已连接
- [ ] 构建设置已配置
- [ ] 首次部署成功
- [ ] 可以访问部署的网站
- [ ] 测试自动部署（推送代码后自动更新）

---

## 🎉 完成！

部署完成后，你的应用就可以在中国正常访问了！

**访问地址示例：**
```
https://english-learning-app.pages.dev
```

如果遇到任何问题，请告诉我！

