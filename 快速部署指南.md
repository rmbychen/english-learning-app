# 🚀 快速部署指南

你的项目已经推送到 GitHub，现在可以通过以下方式部署并获得公共访问地址。

## 方法一：使用 Vercel（推荐 ⭐⭐⭐⭐⭐）

**最简单、最快、免费！**

### 步骤：

1. **访问 Vercel**
   - 打开 https://vercel.com
   - 点击 "Sign Up" 或 "Log In"
   - 使用 **GitHub 账号**登录（推荐）

2. **导入项目**
   - 登录后，点击 "Add New..." → "Project"
   - 在 "Import Git Repository" 中找到 `rmbychen/english-learning-app`
   - 点击 "Import"

3. **配置项目设置**
   - **Framework Preset**: 选择 `Vite`（会自动检测）
   - **Root Directory**: 设置为 `english-learning-app`
   - **Build Command**: `npm run build`（默认）
   - **Output Directory**: `dist`（默认）
   - **Install Command**: `npm install`（默认）

4. **部署**
   - 点击 "Deploy" 按钮
   - 等待 1-2 分钟构建完成

5. **获取访问地址** ✅
   - 部署完成后，你会得到一个 URL，例如：
     - `https://english-learning-app.vercel.app`
     - 或 `https://english-learning-app-xxx.vercel.app`
   - **这个地址就是你的公共访问地址！**

### 优势：
- ✅ 完全免费
- ✅ 自动 HTTPS
- ✅ 自动部署（每次 push 到 GitHub 自动重新部署）
- ✅ 全球 CDN 加速
- ✅ 可以自定义域名

---

## 方法二：使用 Netlify

### 步骤：

1. **访问 Netlify**
   - 打开 https://netlify.com
   - 使用 GitHub 账号登录

2. **导入项目**
   - 点击 "Add new site" → "Import an existing project"
   - 选择 GitHub，授权后选择 `rmbychen/english-learning-app`

3. **配置构建设置**
   - **Base directory**: `english-learning-app`
   - **Build command**: `npm run build`
   - **Publish directory**: `english-learning-app/dist`

4. **部署**
   - 点击 "Deploy site"
   - 等待部署完成

5. **获取访问地址**
   - 格式：`https://random-name-xxx.netlify.app`
   - 可以在设置中自定义域名

---

## 方法三：使用 GitHub Pages（需要配置）

如果你想要使用 GitHub Pages，需要一些额外配置：

1. 修改 `vite.config.ts` 添加 `base` 配置
2. 在 GitHub 仓库设置中启用 Pages
3. 使用 GitHub Actions 自动部署

**注意**：GitHub Pages 部署速度较慢，不推荐用于生产环境。

---

## 📝 部署后的配置

### 配置 Supabase 重定向 URL（重要！）

部署完成后，需要在 Supabase 中配置重定向 URL：

1. 访问你的 Supabase Dashboard
2. 进入 **Authentication** → **URL Configuration**
3. 在 **Redirect URLs** 中添加你的部署地址：
   - 例如：`https://your-app.vercel.app`
   - 例如：`https://your-app.vercel.app/**`
   - 例如：`https://your-app.netlify.app`

这样登录和认证功能才能正常工作。

---

## 🔄 自动部署

部署完成后，每次你推送代码到 GitHub：

```bash
git add .
git commit -m "更新内容"
git push
```

Vercel/Netlify 会自动检测更改并重新部署！✨

---

## 🎯 推荐流程

1. ✅ **已完成**：代码已推送到 GitHub
2. ⏭️ **下一步**：访问 https://vercel.com 部署（5 分钟搞定）
3. ⏭️ **配置**：在 Supabase 中添加部署 URL
4. ✅ **完成**：分享你的应用链接！

---

## 💡 提示

- Vercel 部署最快最简单，推荐使用
- 部署地址可以分享给任何人访问
- 所有部署平台都提供免费 HTTPS
- 可以绑定自定义域名（需要域名）

**现在就去 Vercel 部署吧！** 🚀
